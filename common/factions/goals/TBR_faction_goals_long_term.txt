FACTION_GOAL_STAY_UNITED = {
	name = FACTION_GOAL_STAY_UNITED_NAME
	description = FACTION_GOAL_STAY_UNITED_DESC
	categors = long_term
	group = tbd

	visible = {}
	available = {}

	completed = {
		date > 1945.12.31
		# For each puppet, use this template:
		OR = {
			AND = {
				# owns_state = the puppet's states
			}
			# TAG = {
			#     is_puppet_of = ANI
			# }
		}
		# End of the template.
	}

	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}

	ai_will_do = {
		base = 1
	}
}
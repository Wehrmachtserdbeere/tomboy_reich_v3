ideas = {
	
	mobilization_laws = {
	
		law = yes
		use_list_view = yes

		TBR_IDEA_MANPOWER_disarmed_nation = {
			cost = 150
			removal_cost = -1
			level = 7

			modifier = {
				conscription = 0.01
			}

			cancel_if_invalid = no
		}
		
		TBR_IDEA_MANPOWER_volunteers_only = {
			cost = 150
			removal_cost = -1
			level = 6

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			ai_will_do = {
   				factor = 1

   				modifier = {
					factor = 60
					has_idea = TBR_IDEA_MANPOWER_disarmed_nation
					NOT = {
						has_idea = TBR_IDEA_ECONOMY_isolationism
					}
				}
			}

			modifier = {
				conscription = 0.015
			}

			default = yes

			cancel_if_invalid = no
		}
		
		TBR_IDEA_MANPOWER_conscription = {
			cost = 150
			removal_cost = -1
			level = 5

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			modifier = {
				conscription = 0.05
				training_time_factor = 0.1
			}

			ai_will_do = {
   				factor = 1

				modifier = {
					# Ignore if high on manpower
					factor = 0.5
					manpower_per_military_factory > 5000
				}

				modifier = {
					# Prio if low on manpower
					factor = 20
					manpower_per_military_factory < 2000
				}

				modifier = {
					factor = 0
					NOT = { has_idea = TBR_IDEA_MANPOWER_volunteers_only }
				}
			}

			cancel_if_invalid = no
		}
		
		TBR_IDEA_MANPOWER_extensive_conscription = {
			cost = 150
			removal_cost = -1
			level = 4

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			modifier = {
				conscription = 0.075
			}

			ai_will_do = {
   				factor = 1

				modifier = {
					# Ignore if high on manpower
					factor = 0.5
					manpower_per_military_factory > 3000
				}

				modifier = {
					# Prio if low on manpower
					factor = 20
					manpower_per_military_factory <  800
				}

				# china relies a lot on throwing bodies at the enemy
				modifier = {
					factor = 3

					original_tag = CHI
					has_manpower < 300000
				}

				# soviet also relies a lot on throwing bodies at the enemy
				modifier = {
					factor = 3

					original_tag = SOV
					has_manpower < 300000
				}

				modifier = {
					factor = 5

					original_tag = SOV
					has_war = yes
					has_manpower < 1000000
				}

				modifier = {
					factor = 0
					NOT = { has_idea = TBR_IDEA_MANPOWER_conscription }
				}
			}

			cancel_if_invalid = no
		}

		TBR_IDEA_MANPOWER_required_service = {
			cost = 150
			removal_cost = -1
			level = 3

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
				OR = {
					has_government = anti_anime_reactionism
					has_government = pro_anime_reactionism
					threat = 0.5
					has_war_support > 0.6
					surrender_progress > 0
					has_war = yes
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			modifier = {
				conscription = 0.1
				industrial_capacity_factory = -0.1
				industrial_capacity_dockyard = -0.1
				production_speed_buildings_factor = -0.1
				training_time_factor = 0.2
			}

			cancel_if_invalid = no

			ai_will_do = {
   				factor = 1

				modifier = {
					# Prio if low on manpower
					factor = 20

					manpower_per_military_factory <  700
				}
				modifier = {
					# Lower chance if has decent amount
					factor = 0.5

					manpower_per_military_factory >  1000
				}
				modifier = {
					# Block has plenty manpower
					factor = 0

					NOT = { tag = CHI }
					NOT = { tag = SOV }
					manpower_per_military_factory >  3000
				}

				modifier = { # china relies a lot on throwing bodies at the enemy
					factor = 3

					original_tag = CHI
					has_manpower < 200000
				}

				modifier = {
					factor = 5

					original_tag = SOV
					has_manpower < 400000
				}

				modifier = { # CEU needs this before 2AW, badly
					factor = 10

					original_tag = CEU
					has_manpower < 2000000
				}

				modifier = { # CEU needs this before 2AW, badly
					factor = 10

					original_tag = CEU
					date > 1941.1.1
				}
				
				modifier = { # ANI needs this before 2AW, badly
					factor = 10

					original_tag = ANI
					has_manpower < 2000000
				}

				modifier = { # ANI needs this before 2AW, badly
					factor = 10

					original_tag = ANI
					date > 1941.1.1
				}

				modifier = {
					factor = 0
					NOT = { has_idea = TBR_IDEA_MANPOWER_extensive_conscription }
				}

			}

		}

		TBR_IDEA_MANPOWER_all_adults_serve = {
			cost = 150
			removal_cost = -1
			level = 2

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
				OR = {
					has_government = anti_anime_reactionism
					has_government = pro_anime_reactionism
					threat = 0.75
					has_war_support > 0.75
					surrender_progress > 0.2
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			modifier = {
				conscription = 0.25
				industrial_capacity_factory = -0.3
				industrial_capacity_dockyard = -0.3
				production_speed_buildings_factor = -0.3
				training_time_factor = 0.3
			}

			ai_will_do = {
   				factor = 1
				modifier = {
					# Prio if low on manpower
					factor = 20
					manpower_per_military_factory <  700
				}
				modifier = {
					# Lower chance if has decent amount
					factor = 0.5
					manpower_per_military_factory >  1000
				}
				modifier = {
					# Block has plenty manpower
					factor = 0
					NOT = { tag = CHI }
					NOT = { tag = SOV }
					NOT = { AND = { tag = CEU date > 1940.11.1 } }
					manpower_per_military_factory >  3000
				}

				modifier = { # china relies a lot on throwing bodies at the enemy
					factor = 3

					original_tag = CHI
					has_manpower < 200000
				}

				modifier = {
					factor = 5

					original_tag = SOV
					has_manpower < 400000
				}

				modifier = { # CEU needs this before 2AW, badly
					factor = 10

					original_tag = CEU
					has_manpower < 2000000
				}

				modifier = { # CEU needs this before 2AW, badly
					factor = 10

					original_tag = CEU
					date > 1941.1.1
				}
				
				modifier = { # ANI needs this before 2AW, badly
					factor = 10

					original_tag = ANI
					has_manpower < 2000000
				}

				modifier = { # ANI needs this before 2AW, badly
					factor = 10

					original_tag = ANI
					date > 1941.1.1
				}

				modifier = {
					factor = 0
					NOT = { has_idea = TBR_IDEA_MANPOWER_extensive_conscription }
				}

			}

		}

		TBR_IDEA_MANPOWER_all_serve = {
			cost = 150
			removal_cost = -1
			level = 1

			available = {
				if = {
					limit = {
						original_tag = HUN
					}
					NOT = {
						has_idea = HUN_IDEA_treaty_of_trianon
					}
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
				OR = {
					has_war_support > 0.95
					surrender_progress > 0.7
					else = {
						hidden_trigger = { always = no }
						# NEED TO HAVE AN ALWAYS AS AN END STATEMENT
						# IF URE USING AN IF ELSE IN AN OR STATEMENT
						# OTHERWISE THE EVALUATION BREAKS!!
					}
				}
			}

			modifier = {
				conscription = 0.75
				industrial_capacity_factory = -0.4
				industrial_capacity_dockyard = -0.4
				production_speed_buildings_factor = -0.4
				training_time_factor = 0.5
			}

			cancel_if_invalid = no

			ai_will_do = {
   				factor = 1
				modifier = {
					# Prio if low on manpower
					factor = 20
					manpower_per_military_factory <  500
				}
				modifier = {
					# Lower chance if has decent amount
					factor = 0.5
					manpower_per_military_factory >  1000
				}
				modifier = {
					# Block has plenty manpower
					factor = 0
					manpower_per_military_factory >  5000
				}

				modifier = {
					factor = 0
					NOT = { has_idea = TBR_IDEA_MANPOWER_all_adults_serve }
				}

			}
		}

	}

}